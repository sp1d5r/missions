{
	"name": "@mariozechner/pi-missions",
	"version": "0.1.0",
	"description": "Your engineering org: generic long-running coding agents that plan themselves, work any repo, and report at standups with glanceable artifacts",
	"type": "module",
	"bin": {
		"missions": "dist/cli.js"
	},
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"files": [
		"dist",
		"CHANGELOG.md"
	],
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json && shx chmod +x dist/cli.js",
		"test": "npm run build && node test/invariants.mjs && node test/control.mjs && node test/lifecycle.mjs && node test/bootstrap.mjs && node test/ports.mjs && node test/seats.mjs && node test/setup.mjs && node test/workers.mjs && node test/routines.mjs",
		"dev": "tsgo -p tsconfig.build.json --watch --preserveWatchOutput",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"dependencies": {
		"@chaotic1988/pi-subagent": "^0.2.1",
		"@earendil-works/pi-agent-core": "^0.82.1",
		"@earendil-works/pi-ai": "^0.82.1",
		"@earendil-works/pi-coding-agent": "^0.82.1",
		"chalk": "^5.6.2",
		"jiti": "^2.7.0",
		"typebox": "^1.1.38"
	},
	"devDependencies": {
		"@types/node": "^24.3.0",
		"@typescript/native-preview": "7.0.0-dev.20260120.1",
		"shx": "^0.4.0",
		"typescript": "^5.7.3"
	},
	"keywords": [
		"llm",
		"multi-agent",
		"coding-agent",
		"orchestration",
		"missions",
		"ai"
	],
	"author": "Elijah",
	"license": "MIT",
	"engines": {
		"node": ">=20.0.0"
	}
}
